#include <iostream>
#include "ehemath.h"

using namespace std;

// will not workkkkkl , we need a build system and not an IDE

int main(){

    int x, y;
    cin >> x >> y ;
    int result = Add(x,y);
    cout << "Sum: " << result ;
    return 0;
}